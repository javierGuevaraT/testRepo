package com.unosquare.utils;

public class VariableUtils {

	//Not used now but always required to have utils or helpers in frameworks
	public static String cleanSpaces(String string) {
		return string.replace(" ", "");
	}
}
